import 'package:flutter/material.dart';

class CategoryItemModel {
  String title;
  String query;
  Color bgColor;
  CategoryItemModel(
      {required this.title, required this.query, required this.bgColor});
}
